---
const today = new Date();
---

<footer>
	<div class="info">
		&copy; 1999 - {today.getFullYear()} INeedCoffee is a project by Michael Allen Smith. INeedCoffee.com
		does not track users or collect any personal information. The code for this site is freely available
		on GitHub. Use however you like.
	</div>

	<div class="social-links"></div>
</footer>
<style>
	footer {
		padding: 2em 1em 6em 1em;
		background: linear-gradient(var(--gray-gradient)) no-repeat;
		color: rgb(var(--gray));
		text-align: center;
		/* Distinct background from page content in light mode */
		background-color: #f8f9fa;
	}

	.info {
		max-width: 700px;
		margin: auto;
		padding: 1em;
		color: rgb(var(--gray-dark));
	}

	.social-links {
		display: flex;
		justify-content: center;
		gap: 1em;
		margin-top: 1em;
	}

	.social-links a {
		text-decoration: none;
		color: rgb(var(--gray));
	}

	.social-links a:hover {
		color: rgb(var(--gray-dark));
	}

	/* Dark mode styles for footer */
	:global(.dark-mode) footer {
		background: #161c28; /* Darker than the main dark background */
		background-image: none;
	}

	:global(.dark-mode) .info {
		color: #ffffff; /* White text in dark mode */
	}

	:global(.dark-mode) .social-links a {
		color: #b0b6c6; /* Light gray for icons in dark mode */
	}

	:global(.dark-mode) .social-links a:hover {
		color: #ffffff; /* White on hover in dark mode */
	}

	/* System preference dark mode */
	@media (prefers-color-scheme: dark) {
		:global(:not(.light-mode):not(.dark-mode)) footer {
			background: #161c28; /* Darker than the main dark background */
			background-image: none;
		}

		:global(:not(.light-mode):not(.dark-mode)) .info {
			color: #ffffff; /* White text in dark mode */
		}

		:global(:not(.light-mode):not(.dark-mode)) .social-links a {
			color: #b0b6c6; /* Light gray for icons in dark mode */
		}

		:global(:not(.light-mode):not(.dark-mode)) .social-links a:hover {
			color: #ffffff; /* White on hover in dark mode */
		}
	}
</style>
